Vitest execution order for mcp-guardrails.test.ts:

Outer scope beforeEach hook (line 86): runs before EVERY test
├─ Test 1: "allows a tool when no override matches"
│  └─ Outer beforeEach runs
├─ Test 2: "gates a tool when a per-agent override matches"
│  └─ Outer beforeEach runs
...
├─ Inner describe "MCP hard-deny → AgentLens breach mirror (#55)" (line 247)
│  ├─ Inner beforeEach hook (line 261): runs before EVERY test in this describe
│  ├─ Test: "POSTs the breach to AgentLens..."
│  │  ├─ Outer beforeEach runs first (sets jwtSecret only)
│  │  ├─ Inner beforeEach runs (OVERWRITES with jwtSecret + agentlensUrl + token)
│  │  ├─ Test executes
│  │  └─ Inner afterEach runs: vi.unstubAllGlobals() (NO resetConfig!)
│  │     Config now has agentlensUrl and token from inner test
│  ├─ Test: "does NOT call AgentLens for a deny without a session..."
│  │  ├─ Outer beforeEach runs (sets jwtSecret only) - OVERWRITES previous config
│  │  ├─ Inner beforeEach runs (OVERWRITES again)
│  │  ├─ Test executes
│  │  └─ Inner afterEach runs: vi.unstubAllGlobals()
│  └─ End inner describe
│
└─ Outer afterAll runs (line 92): resetConfig() called ONCE after ALL tests

ACTUAL ISSUE FOUND:
The inner afterEach (line 264) should call resetConfig() so the config doesn't bleed
to the next test. However, the outer beforeEach (line 86) calls setConfig() before every
test, so it OVERWRITES the previous config. This masks the bug.

If tests run in different order (e.g., all inner tests first), then the outer tests
would see the agentlensUrl and agentgateServiceToken from inner tests.
